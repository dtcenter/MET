name: Build Docker Image and Trigger METplus Workflow

on:
  push:
    branches:
      - develop
      - 'main_v[0-9]+.[0-9]+'
    paths-ignore:
      - 'docs/**'

  workflow_dispatch:

env:
  DOCKERHUB_REPO: dtcenter/met-dev

jobs:

  build_met_docker:
    name: Handle Docker Image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Get branch name
        id: get_branch_name
        run: echo branch_name=${GITHUB_REF#refs/heads/} >> $GITHUB_OUTPUT

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Get METbaseimage
        id: get_met_base_image
        run: .github/jobs/get_met_base_image.sh

      - name: Build Docker Image
        run: .github/jobs/build_docker_image.sh
        env:
          SOURCE_BRANCH: ${{ steps.get_branch_name.outputs.branch_name }}-lite
          MET_BASE_REPO: ${{ steps.get_met_base_image.output.met_base_repo }}
          MET_BASE_TAG: ${{ steps.get_met_base_image.output.met_base_tag }}

      - name: Push Docker Image
        run: .github/jobs/push_docker_image.sh
        env:
          SOURCE_BRANCH: ${{ steps.get_branch_name.outputs.branch_name }}-lite
          DOCKER_USERNAME: ${{ vars.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_TOKEN }}

  trigger_metplus:
    name: Trigger METplus testing workflow
    runs-on: ubuntu-latest
    needs: build_met_docker
    steps:
      - uses: dtcenter/metplus-action-trigger-use-cases@v1
        with:
          token: ${{ secrets.METPLUS_BOT_TOKEN }}
